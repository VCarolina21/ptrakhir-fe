generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Recipe {
  id         String   @id @default(uuid())
  title      String
  ingredients String
  steps      String
  imageUrl   String?
  createdAt  DateTime @default(now())
}